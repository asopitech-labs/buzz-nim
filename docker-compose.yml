name: nimino

services:
  postgres:
    image: docker.io/library/postgres:17-alpine
    container_name: nimino-postgres
    environment:
      POSTGRES_USER: nimino
      POSTGRES_PASSWORD: nimino_dev
      POSTGRES_DB: nimino
      PGDATA: /var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - nimino-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nimino"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.nimino.service: "postgres"
      com.nimino.env: "dev"
    restart: unless-stopped

  adminer:
    image: docker.io/library/adminer:latest
    container_name: nimino-adminer
    ports:
      - "127.0.0.1:8082:8080"
    networks:
      - nimino-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      com.nimino.service: "adminer"
      com.nimino.env: "dev"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: nimino-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "127.0.0.1:8180:8080"
    networks:
      - nimino-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.nimino.service: "keycloak"
      com.nimino.env: "dev"
    restart: unless-stopped

  minio:
    image: docker.io/minio/minio:latest
    container_name: nimino-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: nimino_dev
      MINIO_ROOT_PASSWORD: nimino_dev_secret
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - nimino-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      com.nimino.service: "minio"
      com.nimino.env: "dev"
    restart: unless-stopped

  minio-init:
    image: docker.io/minio/mc:latest
    container_name: nimino-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - nimino-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 nimino_dev nimino_dev_secret &&
        mc mb --ignore-existing local/nimino-media &&
        mc anonymous set none local/nimino-media
      "
    labels:
      com.nimino.service: "minio-init"
      com.nimino.env: "dev"
    restart: "no"

  prometheus:
    image: docker.io/prom/prometheus:latest
    container_name: nimino-prometheus
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - nimino-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.nimino.service: "prometheus"
      com.nimino.env: "dev"
    restart: unless-stopped

volumes:
  postgres-data:
    name: nimino-postgres-data
    labels:
      com.nimino.volume: "postgres"
  minio-data:
    name: nimino-minio-data
    labels:
      com.nimino.volume: "minio"
  prometheus-data:
    name: nimino-prometheus-data
    labels:
      com.nimino.volume: "prometheus"

networks:
  nimino-net:
    name: nimino-net
    driver: bridge
    labels:
      com.nimino.network: "dev"
