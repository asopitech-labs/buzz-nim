# Nimino community policy v1

`domain.community.policy` owns two pure decisions:

- `lifecycle`: create/archive/unarchive authority and state transitions.
- `scope`: exact equality between the server host-derived community and a resource community.

Rust supplies verified facts and executes the returned effect. Issue #12 removes the listed Rust policy branches at the incompatible cutover; this contract has no compatibility mode.
